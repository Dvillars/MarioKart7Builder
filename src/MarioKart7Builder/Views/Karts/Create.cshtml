@{
    ViewData["Title"] = "create a kart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using MarioKart7Builder.ViewModels
@model CreateKartViewModel

<h2>Create</h2>
<hr />
<h4>Add a new kart</h4>
@using (Html.BeginForm())
{

    <div class="form-group">
        @Html.LabelFor(model => model.kart.name)
        @Html.TextBoxFor(model => model.kart.name)
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.kart.characterId)
        @Html.DropDownListFor(model => model.kart.character.id , Model.Characters, "default", null)
    </div>

    @*<div class="form-group">
        @Html.LabelFor(model => model.body)
        @Html.DropDownList("BodysId")
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.tire)
        @Html.DropDownList("TiresId")
    </div>

    <div class="form-group">
        @Html.LabelFor(model => model.glider)
        @Html.DropDownList("GlidersId")
    </div>*@

    <input type="submit" value="Add new Kart" />
}
<p>@Html.ActionLink("Back to list", "Index")</p>
